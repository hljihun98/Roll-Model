{
  "name": "rollmodel", "version": "1.1.0", "private": true,
  "description": "Offline caster contact and thermal screening tool",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.cjs",
    "build:check": "node scripts/build.cjs --check",
    "start": "node scripts/serve.cjs",
    "test": "node tests/physics.cjs && node --test tests/regressions.cjs tests/build.test.cjs",
    "test:ui": "node tests/smoke.cjs && node tests/verify-ui.cjs && node tests/responsive.cjs",
    "check": "npm run build:check && npm test && npm run test:ui"
  },
  "devDependencies": { "playwright": "1.63.0" }
}
